. $HOME/.bash/src/git-completion
. $HOME/.bash/src/colors
. $HOME/.bash/src/environment_setup
. $HOME/.bash/src/prompt
. $HOME/.bash/src/aliases
. $HOME/.bash/src/functions
. $HOME/.bash/src/cd_hook
. $HOME/.bash/src/path

BASH_COMPLETION=$HOME/.bash/src/bash_completion
if [ -f $BASH_COMPLETION ]; then
  . $BASH_COMPLETION
fi

#KEEP AT END FOR RVM
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
