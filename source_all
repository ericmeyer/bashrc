. $HOME/.bash_completion.d/git-completion.bash
. $HOME/.bash/colors
. $HOME/.bash/environment_setup
. $HOME/.bash/prompt
. $HOME/.bash/aliases
. $HOME/.bash/functions

for file in $HOME/.bash/machine_specific/*
do
  . $file
done

BASH_COMPLETION=/usr/local/etc/bash_completion
if [ -f $BASH_COMPLETION ]; then
   . $BASH_COMPLETION
fi

#KEEP AT END FOR RVM
[[ -s "/Users/ericmeyer/.rvm/scripts/rvm" ]] && source "/Users/ericmeyer/.rvm/scripts/rvm"